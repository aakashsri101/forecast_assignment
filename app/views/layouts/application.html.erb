<!DOCTYPE html>
<html>
  <head>
    <title>Weather Forecast Application</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        padding: 20px;
      }

      .container {
        max-width: 900px;
        margin: 0 auto;
      }

      .header {
        text-align: center;
        color: white;
        margin-bottom: 40px;
      }

      .header h1 {
        font-size: 2.5rem;
        margin-bottom: 10px;
        font-weight: 700;
      }

      .header p {
        font-size: 1.1rem;
        opacity: 0.9;
      }

      .card {
        background: white;
        border-radius: 20px;
        padding: 30px;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        margin-bottom: 20px;
      }

      .flash {
        padding: 15px 20px;
        border-radius: 10px;
        margin-bottom: 20px;
        font-weight: 500;
      }

      .flash.error {
        background: #fee;
        color: #c33;
        border: 1px solid #fcc;
      }

      .flash.success {
        background: #efe;
        color: #3c3;
        border: 1px solid #cfc;
      }

      .flash.notice {
        background: #eef;
        color: #33c;
        border: 1px solid #ccf;
      }

      .cache-indicator {
        display: inline-block;
        padding: 6px 12px;
        border-radius: 6px;
        font-size: 0.85rem;
        font-weight: 600;
        margin-left: 10px;
      }

      .cache-indicator.cached {
        background: #d4edda;
        color: #155724;
        border: 1px solid #c3e6cb;
      }

      .cache-indicator.fresh {
        background: #cce5ff;
        color: #004085;
        border: 1px solid #b8daff;
      }

      footer {
        text-align: center;
        color: white;
        margin-top: 40px;
        opacity: 0.8;
        font-size: 0.9rem;
      }

      @media (max-width: 768px) {
        .header h1 {
          font-size: 2rem;
        }

        .card {
          padding: 20px;
        }
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="header">
        <h1>🌤️ Weather Forecast</h1>
        <p>Get accurate weather information for any address</p>
      </div>

      <% if flash[:error] %>
        <div class="flash error">
          <strong>Error:</strong> <%= flash[:error] %>
        </div>
      <% end %>

      <% if flash[:notice] %>
        <div class="flash notice">
          <%= flash[:notice] %>
        </div>
      <% end %>

      <% if flash[:success] %>
        <div class="flash success">
          <%= flash[:success] %>
        </div>
      <% end %>

      <%= yield %>

      <footer>
        <p>Built with Ruby on Rails | Enterprise-Grade Weather Forecast Application</p>
        <p style="margin-top: 10px; font-size: 0.8rem;">
          Data cached for 30 minutes per zip code for optimal performance
        </p>
      </footer>
    </div>
  </body>
</html>

